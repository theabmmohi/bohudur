import type { BohudurOptions, QueryRequest, QueryResponse } from "@type/index"
import { Request } from "@bohudur/http"

export default function query(key: string, options: BohudurOptions, request: QueryRequest): Promise<QueryResponse> {
  
}
