import type { BohudurOptions, ExecuteRequest, ExecuteResponse } from "@type/index"
import { Request } from "@bohudur/http"

export default function execute(key: string, options: BohudurOptions, request: ExecuteRequest): Promise<ExecuteResponse> {
  
}
