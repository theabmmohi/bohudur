import type { BohudurOptions, CreateRequest, CreateResponse } from "@type/index"
import { Request } from "@bohudur/http"

export default function create(key: string, options: BohudurOptions, request: CreateRequest): Promise<CreateResponse> {
  
}
